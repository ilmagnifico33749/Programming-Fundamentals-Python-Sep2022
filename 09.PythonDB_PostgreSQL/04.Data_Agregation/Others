-- SELECT * FROM employees
-- ;


SELECT department_name,
	COUNT(department_name) AS "No. of employees",
	MAX("salary") AS "Highest salary"
FROM employees
GROUP BY department_name, salary
ORDER BY salary DESC
;
